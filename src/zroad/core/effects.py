"""effects.py —— 卡牌事件解析与结算（M3 实现，M2 预留骨架）。

职责（M3 展开）：
    1. 把 cards.json 的 event_raw 文本解析成结构化“效果指令”，60 张卡全部可解析
       或被显式标记为“需人工实现”，禁止静默忽略；
    2. 区分黑字事件（立即生效，如资源增减、队伍 ±1、支付抉择）与
       红字事件（影响本场战斗的修正，如“远程掷出咬伤则丧尸+1”“本场不能逃跑”）；
    3. 六种特殊道具（狙击枪/地图/校车/毒气/受伤/幸存者）的获得、持有与触发消耗。
"""

# M3 实现。
